source 'https://github.com/CocoaPods/Specs.git'
#source 'git@github.com:metacosm/ShadowrunModel.git'

# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'ShadowrunAssistant' do
  workspace 'ShadowrunAssistant'
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ShadowrunAssistant

  pod 'SwiftSoup'
  # pod 'Kanna', :git => 'https://github.com/tid-kijyun/Kanna', :branch => 'feature/v3.0.0'
  # pod 'RealmSwift'
  # pod 'ShadowrunModel', '~> 0.0.1'

  target 'ShadowrunAssistantTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ShadowrunAssistantUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

#post_install do |installer|
#   installer.pods_project.targets.each do |target|
#      target.build_configurations.each do |config|
#         config.build_settings['SWIFT_VERSION'] = '3.1'
#      end
#   end
#end
